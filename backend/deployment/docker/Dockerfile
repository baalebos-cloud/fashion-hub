# See ../../Dockerfile (backend/Dockerfile) for the authoritative, actively
# used image definition. This placeholder exists so deployment tooling that
# expects a Dockerfile under deployment/docker/ has one to reference;
# keep both in sync, or point CI/CD at backend/Dockerfile directly.
