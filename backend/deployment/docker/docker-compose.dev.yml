# Development overrides: mount source for live-reload, verbose logging.
version: "3.9"
services:
  api:
    volumes:
      - ../../:/app
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    environment:
      APP_ENV: development
      DEBUG: "true"
