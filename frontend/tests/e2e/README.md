# End-to-end tests

Real-browser tests (Playwright/Cypress) against a running instance of both
frontend and backend. Not implemented in this scaffold. Priority
candidates once set up: signup -> login, the full customer order lifecycle
through "Mark as Received," and the multi-vendor checkout flow.
