# Integration tests

End-to-end-ish tests that exercise a full flow (e.g. login -> view orders
-> mark received) against a mocked API layer (recommend MSW). None are
implemented yet in this scaffold — see backend/tests/ for the equivalent
backend-side coverage of the same business rules.
